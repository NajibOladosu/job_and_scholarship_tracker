# Railway Environment Variables
# Copy these to Railway's environment variables section

# Django Settings
DJANGO_SETTINGS_MODULE=config.settings.production
DEBUG=False
ALLOWED_HOSTS=.railway.app

# Security - IMPORTANT: Generate a new secret key!
# Run: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=REPLACE-WITH-YOUR-GENERATED-SECRET-KEY-MINIMUM-50-CHARS

# Google Gemini API
# Get your key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=REPLACE-WITH-YOUR-GEMINI-API-KEY

# Email Configuration (Optional - for password reset and notifications)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-gmail-app-password

# Security Settings (Production)
CSRF_COOKIE_SECURE=True
SESSION_COOKIE_SECURE=True
SECURE_SSL_REDIRECT=True

# NOTE: DATABASE_URL and REDIS_URL are automatically set by Railway
# Do NOT add these manually - Railway provides them when you add PostgreSQL and Redis services
